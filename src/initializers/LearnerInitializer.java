package initializers;

import peersim.core.Control;

/**
 * Control to initialize the learning algorithm. Do here all the stuff that must be done only once and
 * before the simulation starts.
 * 
 */
public class LearnerInitializer implements Control {

	@Override
	public boolean execute() {
		return false;
	}

}
